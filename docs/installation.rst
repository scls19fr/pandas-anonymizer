.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install pandas_anonymizer

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pandas_anonymizer
    $ pip install pandas_anonymizer
